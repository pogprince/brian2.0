import type { Metadata } from 'next'
import '../globals.css'

export const metadata: Metadata = {
  title: 'Register — Brain',
}

export default function RegisterLayout({ children }: { children: React.ReactNode }) {
  return children
}
